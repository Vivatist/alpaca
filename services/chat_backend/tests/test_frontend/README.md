# ALPACA RAG Test Console

Интерактивный фронтенд для тестирования RAG запросов с поддержкой SSE streaming.

## Возможности

- 🔄 **Потоковый режим (SSE)** — ответы отображаются в реальном времени
- 📋 **Предзагруженные тестовые запросы** — из `queries.js`
- ▶️ **Автоматический прогон** — запуск всех выбранных запросов последовательно
- 📊 **Таблица результатов** — фильтры, найдено документов, время, ответ
- ✏️ **Ручной режим** — отправка произвольных запросов
- 💾 **Сохранение запросов** — добавленные/удалённые запросы сохраняются в файл

## Запуск

### Вариант 1: Node.js сервер (рекомендуется)

С сохранением добавленных/удалённых запросов в файл `queries.js`:

**Windows:**
```bash
cd services/chat_backend/tests/test_frontend
start.bat
```

**Linux/macOS:**
```bash
cd services/chat_backend/tests/test_frontend
./start.sh
```

Или напрямую:
```bash
node server.js
```

Открыть: http://localhost:8888

### Вариант 2: Простой HTTP-сервер (Python)

Без сохранения запросов (только в памяти):

```bash
cd services/chat_backend/tests/test_frontend
python -m http.server 8888
```

Открыть: http://localhost:8888

### Вариант 3: Live Server (VS Code)

1. Установить расширение "Live Server"
2. Правый клик на `index.html` → "Open with Live Server"

## API (при запуске через server.js)

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/queries` | Получить список запросов |
| POST | `/api/queries` | Добавить запрос (`{"query": "текст"}`) |
| DELETE | `/api/queries/:id` | Удалить запрос по индексу |

## Настройка

- **API URL**: по умолчанию `http://localhost:8082/api/chat`
- **Backend**: загружается динамически с сервера через `GET /api/backends`

## Структура

```
test_frontend/
├── index.html      # Главная страница
├── styles.css      # Стили (dark theme)
├── queries.js      # Предзагруженные тестовые запросы (автообновляется)
├── app.js          # Логика приложения (SSE, UI)
├── server.js       # Node.js сервер с API для сохранения запросов
├── start.bat       # Скрипт запуска (Windows)
├── start.sh        # Скрипт запуска (Linux/macOS)
└── README.md       # Документация
```

## Добавление запросов

Отредактируйте `queries.js`:

```javascript
const DEFAULT_QUERIES = [
    "Ваш новый запрос",
    // ...
];
```

Или добавьте через интерфейс (поле внизу списка запросов).
