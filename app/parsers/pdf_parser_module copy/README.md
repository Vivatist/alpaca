# PDF Parser для ALPACA RAG

Парсер PDF документов с поддержкой OCR для отсканированных страниц.

## Возможности

- ✅ **Текстовые PDF**: Извлечение текста с сохранением структуры
- ✅ **Отсканированные PDF**: OCR распознавание с русским + английским языками
- ✅ **Метаданные**: Автор, даты, количество страниц, шифрование
- ✅ **YAML Header**: Метаданные для RAG индексации
- ✅ **Автоопределение типа**: Текстовый или отсканированный документ

## Этапы парсинга PDF

### 1️⃣ **Извлечение метаданных** (`_extract_metadata`)
**Цель**: Получить информацию о документе

- Библиотека: `pypdf`
- Извлекаемые данные:
  - Title, Author, Subject, Creator, Producer
  - Даты создания/модификации (парсинг PDF формата `D:YYYYMMDDHHmmSS`)
  - Количество страниц
  - Статус шифрования
- Fallback: Базовые метаданные (имя файла из пути)

### 2️⃣ **Определение типа PDF** (`_is_scanned_pdf`)
**Цель**: Понять нужен ли OCR

**Стратегия**:
- Извлекаем текст с первых 3 страниц через `pypdf.extract_text()`
- Если в среднем < 50 символов на страницу → отсканированный PDF
- Если текста достаточно → текстовый PDF

**Логика**:
```python
avg_chars_per_page = total_text_length / pages_checked
is_scanned = avg_chars_per_page < 50
```

### 3️⃣ **Основной парсинг** (выбор стратегии)

#### 3A. **Текстовый PDF** → `_parse_with_markitdown`
- **Приоритет**: Markitdown (сохраняет форматирование)
- **Fallback**: `_fallback_parse()` через `pypdf`
  - Постраничное извлечение текста
  - Формат: `## Страница N\n\n{текст}`

#### 3B. **Отсканированный PDF** → `_parse_with_ocr`
- **Библиотека**: Unstructured (`partition_pdf`)
- **Параметры**:
  ```python
  partition_pdf(
      filename=file_path,
      strategy=self.ocr_strategy,  # 'auto', 'hi_res', 'fast', 'ocr_only'
      infer_table_structure=True,  # Распознавание таблиц
      languages=["rus", "eng"],    # КРИТИЧНО: русский + английский
      extract_images_in_pdf=True,  # Извлечение изображений
      max_partition=None           # Обрабатывать все страницы
  )
  ```
- **Процесс**:
  1. OCR обработка всех страниц
  2. Группировка элементов по номерам страниц (из `element.metadata.page_number`)
  3. Формирование Markdown с разделением: `## Страница N`
  4. Конкатенация всех страниц

#### 3C. **Гибридный режим** (текст + OCR)
**Условие**: Текстовый PDF, но мало контента (`len(text) < 100`)

- Запускаем дополнительно OCR
- Сравниваем длину результатов
- Выбираем более полный результат

### 4️⃣ **Генерация YAML Header** (`_generate_yaml_header`)
**Цель**: Метаданные для RAG индексации

```yaml
---
document_type: pdf
file_name: Договор_123.pdf
file_path: /path/to/file.pdf
parsed_date: 2025-10-28T14:30:00Z
parser: alpaca-pdf-parser
title: "Договор поставки оборудования"
author: "ООО Георезонанс"
subject: "Контракт"
created: 2024-05-15T10:30:00
modified: 2024-06-20T15:45:00
pages: 25
encrypted: false
ocr_enabled: true
is_scanned: true
ocr_strategy: auto
---
```

**Назначение**: Трассировка источников в RAG ответах (файл + страница)

### 5️⃣ **Финальная сборка результата**

**Структура возвращаемого словаря**:
```python
{
    'markdown': str,          # Текст в Markdown формате
    'metadata': Dict,         # Метаданные из этапа 1
    'pages_info': List[Dict], # Информация о страницах
    'yaml_header': str,       # YAML header из этапа 4
    'success': bool,          # True/False
    'error': Optional[str]    # Сообщение при ошибке
}
```

## Зависимости

```bash
# Основные
pypdf>=3.17.0              # Современный PDF парсер
markitdown[all]>=0.0.1a2   # Microsoft Markdown конвертер
unstructured>=0.10.0       # OCR + структурирование документов
pdf2image>=1.16.0          # PDF → изображения (для OCR)

# OCR движок
pytesseract>=0.3.10        # Tesseract wrapper
pillow>=10.0.0             # Обработка изображений

# Системные зависимости (Docker)
tesseract-ocr              # OCR движок
tesseract-ocr-rus          # Русский языковой пакет
tesseract-ocr-eng          # Английский языковой пакет
poppler-utils              # pdf2image backend
```

## Использование

### Из Celery задачи
```python
from parsers.pdf.pdf_parser import PDFParser

# Инициализация с OCR
parser = PDFParser(enable_ocr=True, ocr_strategy='auto')

# Парсинг
result = parser.parse('/path/to/document.pdf')

if result['success']:
    # Сохранение в Markdown
    parser.save_to_markdown_file(result, '/output/document.md')
    
    print(f"Pages: {result['metadata']['pages']}")
    print(f"Author: {result['metadata']['author']}")
    print(f"Content length: {len(result['markdown'])}")
```

### Стратегии OCR

| Стратегия | Описание | Скорость | Точность |
|-----------|----------|----------|----------|
| `auto` | Автоматический выбор | Средняя | Высокая |
| `fast` | Быстрая обработка без OCR | Высокая | Низкая (только текст) |
| `hi_res` | Высокое качество OCR | Низкая | Максимальная |
| `ocr_only` | Только OCR, игнорировать текстовый слой | Низкая | Высокая |

**Рекомендация**: `auto` - оптимальный баланс

## Особенности реализации

### 🔍 Автоопределение типа документа
Парсер **сам определяет** нужен ли OCR, проверяя количество текста на первых страницах.

### 📄 Постраничная структура
Все результаты структурированы по страницам:
```markdown
## Страница 1

Текст первой страницы...

## Страница 2

Текст второй страницы...
```

### 🌐 Поддержка русского языка
**КРИТИЧНО**: `languages=["rus", "eng"]` обязательно указывается для OCR, т.к. документы ООО "Георезонанс" преимущественно на русском.

### ⚡ Graceful Degradation
На каждом этапе есть fallback:
- Нет Markitdown → pypdf
- Нет Unstructured → pypdf без OCR
- Нет pypdf → сообщение об ошибке

### 📊 Логирование
Все этапы логируются через `alpaca_logger` с метриками:
```python
logger.info(f"PDF parsed successfully | file={path} pages={metadata['pages']} content_length={len(markdown)} duration={time:.2f}s")
```

## Сравнение с Word парсером

| Аспект | PDF Parser | Word Parser |
|--------|------------|-------------|
| Основной парсер | Markitdown + Unstructured | Markitdown + python-docx |
| OCR | `partition_pdf` (встроенный) | `partition_image` (извлеченные) |
| Конвертация формата | Не требуется | .doc → .docx (LibreOffice) |
| Изображения | Встроены в PDF | Извлекаются отдельно + WMF→PNG |
| Автоопределение OCR | ✅ Да (`_is_scanned_pdf`) | ❌ Нет (всегда ищет изображения) |
| Постраничность | ✅ Встроенная (из metadata) | ❌ Нет (документ целиком) |

## Celery интеграция

Парсер вызывается из `document-processors/src/tasks/pdf_tasks.py`:

```python
@app.task(bind=True)
def process_pdf_file(self, file_path: str, message: Dict) -> Dict:
    """Process PDF file with PDFParser - Celery task"""
    
    # 1. Парсинг
    parse_result = pdf_parser.parse(file_path)
    
    # 2. Генерация имени markdown файла
    timestamp = datetime.utcnow().strftime('%Y%m%d_%H%M%S_%f')[:-3]
    md_filename = f"{timestamp}_{safe_name}.md"
    
    # 3. Сохранение в /volume_md
    md_file_path = Path('/volume_md') / md_filename
    pdf_parser.save_to_markdown_file(parse_result, str(md_file_path))
    
    # 4. Возврат результата с метаданными
    return {
        'status': 'success',
        'markdown_file': md_filename,
        'metadata': parse_result['metadata'],
        'processing_time_sec': round(processing_time, 2)
    }
```

## Тестирование

```bash
# Текстовый PDF
python -c "
from parsers.pdf.pdf_parser import PDFParser
parser = PDFParser(enable_ocr=False)
result = parser.parse('test_textual.pdf')
print(result['metadata'])
"

# Отсканированный PDF (с OCR)
python -c "
from parsers.pdf.pdf_parser import PDFParser
parser = PDFParser(enable_ocr=True, ocr_strategy='hi_res')
result = parser.parse('test_scanned.pdf')
print(f'Pages: {result[\"metadata\"][\"pages\"]}')
print(f'Content length: {len(result[\"markdown\"])}')
"
```

## TODO

- [ ] Поддержка защищенных PDF (password)
- [ ] Извлечение ссылок и закладок
- [ ] Обработка форм (AcroForms)
- [ ] Извлечение встроенных файлов
- [ ] Кеширование OCR результатов
- [ ] Параллельная обработка страниц
