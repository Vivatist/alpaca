# Word Parser - Этапы обработки документов

## 📊 Схема процесса парсинга Word

```
┌─────────────────────────────────────────────────────────────────┐
│                 ВХОДНОЙ WORD ФАЙЛ (.doc/.docx)                   │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│  ЭТАП 1: Конвертация формата (опционально)                      │
│  • Проверка расширения: .doc → требуется конвертация            │
│  • LibreOffice headless: --convert-to docx                      │
│  • Временная директория: /tmp/alpaca_doc_convert_*/            │
│  • Результат: .docx файл для дальнейшей обработки               │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│  ЭТАП 2: Извлечение метаданных (python-docx)                    │
│  • Title, Author, Subject (из core_properties)                  │
│  • Даты создания/модификации                                     │
│  • Количество параграфов, таблиц                                │
│  • Приблизительное количество страниц (250 слов/стр)            │
│  • Подсчет изображений (через doc.part.rels)                    │
│  • Fallback для .doc: базовые метаданные                        │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│  ЭТАП 3: Извлечение изображений (если OCR включен)              │
│  • Итерация по doc.part.rels (связи с медиа)                    │
│  • Фильтр: "image" in rel.target_ref                            │
│  • Извлечение бинарных данных: rel.target_part.blob             │
│  • Определение расширения по MIME (content_type)                │
│  • Сохранение в /tmp/alpaca_word_images_*/                      │
│  • КРИТИЧНО: Конвертация WMF/EMF → PNG                          │
│    - Попытка 1: ImageMagick (magick/convert)                    │
│    - Попытка 2: PIL (прямое открытие)                           │
│  • Результат: [{index, path, size, type}, ...]                  │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│  ЭТАП 4: Основной парсинг структуры документа                   │
│  • Приоритет: Markitdown (сохраняет форматирование)             │
│    - Заголовки, списки, таблицы                                 │
│    - Полная структура документа                                 │
│  • Fallback: python-docx                                         │
│    - Обход doc.element.body (CT_P, CT_Tbl)                      │
│    - Распознавание заголовков (Heading 1-9)                     │
│    - Конвертация таблиц в Markdown                              │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│  ЭТАП 5: OCR обработка изображений (если включен + есть images) │
│  • Библиотека: Unstructured (partition_image)                   │
│  • Параметры:                                                    │
│    - strategy=auto/hi_res/fast                                  │
│    - infer_table_structure=True                                 │
│    - languages=["rus", "eng"] ← КРИТИЧНО для русского           │
│  • Процесс для каждого изображения:                             │
│    1. Проверка существования/размера файла                      │
│    2. partition_image() → OCR распознавание                     │
│    3. Извлечение текста из элементов                            │
│    4. Форматирование: ### Изображение N                         │
│    5. Очистка временных файлов (os.remove)                      │
│  • Добавление секции: "## Распознанный текст из изображений"   │
│  • Логирование: successful/failed/total                         │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│  ЭТАП 6: Генерация YAML Header                                  │
│  ---                                                             │
│  document_type: word                                             │
│  file_name: Договор_123.docx                                     │
│  parsed_date: 2025-10-28T14:30:00Z                              │
│  parser: alpaca-word-parser                                      │
│  title: "Договор поставки"                                       │
│  author: "ООО Георезонанс"                                       │
│  pages: 12                                                       │
│  paragraphs: 156                                                 │
│  tables: 3                                                       │
│  images: 2                                                       │
│  ocr_enabled: true                                               │
│  ---                                                             │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│  ЭТАП 7: Финальная сборка результата + очистка                  │
│  • Компоновка:                                                   │
│    {                                                             │
│      'markdown': 'основной текст + OCR секция',                 │
│      'metadata': {...},                                          │
│      'images': [{...}, ...],                                     │
│      'yaml_header': '---\n...',                                  │
│      'success': True                                             │
│    }                                                             │
│  • Финальная очистка (блок finally):                            │
│    - Удаление временной директории .doc → .docx                 │
│    - Временные изображения уже удалены в этапе 5                │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│           СОХРАНЕНИЕ В MARKDOWN (Celery task)                   │
│  • Генерация имени: {timestamp}_{safe_name}.md                  │
│  • Путь: /volume_md/{filename}                                  │
│  • Формат: YAML Header + Markdown Content                       │
└─────────────────────────────────────────────────────────────────┘
```

## 🔑 Ключевые особенности

### 1. Двойное преобразование для .doc
Устаревший формат `.doc` сначала конвертируется в `.docx` через LibreOffice headless:
```bash
libreoffice --headless --convert-to docx --outdir /tmp file.doc
```
- **Timeout**: 60 секунд
- **Очистка**: Автоматическое удаление временной директории

### 2. Обязательная конвертация WMF/EMF
Windows Metafile форматы не поддерживаются PIL/Tesseract напрямую:
- **Стратегия 1**: ImageMagick (`magick` или `convert`) → PNG
- **Стратегия 2**: Попытка открыть через PIL (некоторые WMF содержат BMP)
- **Результат**: PNG файлы для OCR

### 3. Постраничная обработка изображений
Каждое изображение обрабатывается отдельно:
```markdown
## Распознанный текст из изображений

### Изображение 1

Распознанный текст первого изображения...

### Изображение 2

Распознанный текст второго изображения...
```

### 4. Поддержка русского языка
**КРИТИЧНО**: `languages=["rus", "eng"]` для OCR, т.к. контракты ООО "Георезонанс" на русском.

### 5. Graceful Degradation
Каждый этап имеет fallback:
- Нет Markitdown → python-docx
- Нет LibreOffice → прямой парсинг .doc (с предупреждением)
- Нет ImageMagick → PIL (для WMF конвертации)
- Нет Unstructured → пропуск OCR

## 📦 Зависимости vs PDF Parser

| Библиотека | Word Parser | PDF Parser |
|------------|-------------|------------|
| Основной парсер | `markitdown` | `markitdown` |
| Метаданные | `python-docx` | `pypdf` |
| OCR движок | `unstructured.partition_image` | `unstructured.partition_pdf` |
| Конвертация формата | LibreOffice (headless) | ❌ Не требуется |
| Обработка изображений | `PIL`, ImageMagick | `pdf2image` |
| Извлечение изображений | ✅ Ручное (из rels) | ❌ Встроено в partition_pdf |

## 🎯 Сравнение процессов

### Word: 7 этапов
1. **Конвертация** .doc → .docx (опционально, LibreOffice)
2. **Метаданные** (python-docx, core_properties)
3. **Извлечение изображений** (doc.part.rels + WMF→PNG)
4. **Парсинг структуры** (Markitdown или python-docx)
5. **OCR изображений** (Unstructured partition_image)
6. **YAML header** (метаданные для RAG)
7. **Сборка + очистка** (удаление временных файлов)

### PDF: 5 этапов
1. Метаданные (pypdf)
2. Определение типа (текст vs скан)
3. Парсинг (Markitdown или OCR)
4. YAML header
5. Сборка результата

**Основная разница**: Word требует предварительного извлечения изображений, PDF обрабатывает их встроенно.

## 🚀 Производительность

### .docx без изображений
- **Скорость**: ~0.5-1 секунда на документ
- **Стратегия**: Markitdown → прямой парсинг
- **Использование CPU**: Низкое

### .docx с изображениями (OCR включен)
- **Скорость**: ~2-5 секунд/изображение
- **Стратегия**: Извлечение → конвертация WMF → OCR
- **Использование CPU**: Высокое (OCR)

### .doc формат (с конвертацией)
- **Дополнительное время**: +5-15 секунд (LibreOffice)
- **Зависимость**: Размер документа
- **Лимит**: Timeout 60 секунд

### Рекомендации по OCR стратегиям
- **`auto`** - для production (баланс скорости и качества)
- **`fast`** - для тестирования (минимальный OCR)
- **`hi_res`** - для важных отсканированных документов

## 💡 Использование в Celery

```python
# В word_tasks.py
from parsers.word.word_parser import WordParser

word_parser = WordParser(enable_ocr=True, ocr_strategy='auto')

@app.task(bind=True)
def process_word_file(self, file_path: str, message: Dict) -> Dict:
    # Парсинг (автоматическая конвертация .doc если нужно)
    parse_result = word_parser.parse(file_path)
    
    if not parse_result['success']:
        return {'status': 'error', 'error': parse_result['error']}
    
    # Генерация имени markdown
    timestamp = datetime.utcnow().strftime('%Y%m%d_%H%M%S_%f')[:-3]
    md_filename = f"{timestamp}_{safe_name}.md"
    
    # Сохранение в /volume_md
    md_path = Path('/volume_md') / md_filename
    word_parser.save_to_markdown_file(parse_result, str(md_path))
    
    return {
        'status': 'success',
        'markdown_file': md_filename,
        'metadata': parse_result['metadata'],
        'images_count': len(parse_result['images'])
    }
```

## 🔧 Конфигурация

### Включение/отключение OCR
```python
# Без OCR (только текст)
parser = WordParser(enable_ocr=False)

# С OCR (для изображений в документе)
parser = WordParser(enable_ocr=True, ocr_strategy='auto')

# Максимальное качество OCR
parser = WordParser(enable_ocr=True, ocr_strategy='hi_res')
```

### Mock режим (для тестирования)
```yaml
# shared/config/main_config.yaml
document_processors:
  mock_parsers:
    enabled: true  # Использовать MockParser вместо реального
```

## 📝 Формат вывода

### Markdown файл
```markdown
---
document_type: word
file_name: Договор_123.docx
file_path: /volume_documents/Договор_123.docx
parsed_date: 2025-10-28T14:30:00Z
parser: alpaca-word-parser
title: "Договор поставки оборудования"
author: "ООО Георезонанс"
created: 2024-05-15T10:30:00
modified: 2024-06-20T15:45:00
pages: 12
paragraphs: 156
tables: 3
images: 2
ocr_enabled: true
---

# ДОГОВОР ПОСТАВКИ №123

от 15 мая 2024 года

Настоящий договор заключен между...

## 1. ПРЕДМЕТ ДОГОВОРА

1.1. Поставщик обязуется...

| Наименование | Количество | Цена |
|-------------|-----------|------|
| Оборудование А | 10 шт | 50000 ₽ |
| Оборудование Б | 5 шт | 75000 ₽ |

## Распознанный текст из изображений

### Изображение 1

[Текст из отсканированной подписи или печати]

### Изображение 2

[Текст из вложенной схемы или диаграммы]
```

### Celery результат
```python
{
    'status': 'success',
    'file_name': 'Договор_123.docx',
    'file_type': 'word',
    'processing_time_sec': 4.23,
    'processed_timestamp': '2025-10-28T14:30:00Z',
    'markdown_file': '20251028_143000_123_Dogovor_123.md',
    'markdown_path': '/volume_md/20251028_143000_123_Dogovor_123.md',
    'markdown_length': 8542,
    'metadata': {
        'title': 'Договор поставки оборудования',
        'author': 'ООО Георезонанс',
        'subject': 'Контракт',
        'created': '2024-05-15T10:30:00',
        'modified': '2024-06-20T15:45:00',
        'pages': 12,
        'paragraphs': 156,
        'tables': 3,
        'images': 2
    },
    'images_count': 2,
    'ocr_enabled': True,
    'message': 'Word document parsed and saved to 20251028_143000_123_Dogovor_123.md in 4.23s',
    'task_id': 'xyz789-abc123'
}
```

## 🔍 Специфические особенности Word

### WMF/EMF изображения
Windows Metafile - проприетарный формат Microsoft:
- **Проблема**: Не поддерживается PIL/Tesseract напрямую
- **Решение**: Конвертация через ImageMagick в PNG
- **Fallback**: Попытка открыть как обычное изображение через PIL

### Структура документа
Word хранит документ как XML структуру:
```python
doc.element.body → [CT_P, CT_Tbl, CT_P, ...]
```
- **CT_P** (Paragraph) → текстовый параграф
- **CT_Tbl** (Table) → таблица
- **Heading styles** → распознаются и конвертируются в Markdown заголовки

### Метаданные из .doc
Старый формат `.doc` (бинарный):
- **Ограничение**: python-docx не поддерживает чтение метаданных
- **Решение**: Базовые метаданные (имя файла, "Unknown" автор)
- **Рекомендация**: Конвертировать в .docx для полных метаданных

## ⚙️ Системные зависимости (Docker)

```dockerfile
# LibreOffice для конвертации .doc → .docx
RUN apt-get update && apt-get install -y libreoffice

# ImageMagick для WMF → PNG
RUN apt-get install -y imagemagick

# Tesseract OCR с русским языком
RUN apt-get install -y tesseract-ocr tesseract-ocr-rus tesseract-ocr-eng

# Python библиотеки
RUN pip install markitdown python-docx unstructured[all-docs] pillow pytesseract
```

## 🐛 Типичные проблемы и решения

### 1. LibreOffice не установлен
**Симптом**: Ошибка конвертации .doc файлов
**Решение**: 
```bash
apt-get install -y libreoffice
```

### 2. ImageMagick не найден
**Симптом**: WMF изображения не конвертируются
**Решение**:
```bash
apt-get install -y imagemagick
```
**Fallback**: Попытка через PIL (ограниченная поддержка)

### 3. OCR не работает для русского текста
**Симптом**: Пустой или некорректный текст из изображений
**Решение**: Проверить языковые пакеты:
```bash
apt-get install -y tesseract-ocr-rus tesseract-ocr-eng
```

### 4. Timeout при конвертации больших .doc
**Симптом**: LibreOffice зависает на больших файлах
**Решение**: Увеличить timeout в `_convert_doc_to_docx`:
```python
subprocess.run(..., timeout=120)  # было 60
```

## 📊 Сравнительная таблица: Word vs PDF

| Характеристика | Word Parser | PDF Parser |
|---------------|-------------|------------|
| **Этапов обработки** | 7 | 5 |
| **Конвертация формата** | ✅ .doc → .docx | ❌ Не требуется |
| **Извлечение изображений** | ✅ Ручное | ❌ Автоматическое |
| **WMF конвертация** | ✅ ImageMagick | ❌ Не нужна |
| **Автоопределение OCR** | ❌ Всегда если images | ✅ По содержимому |
| **Постраничность** | ❌ Документ целиком | ✅ Встроенная |
| **OCR метод** | partition_image | partition_pdf |
| **Таблицы** | ✅ Markdown таблицы | ✅ infer_table_structure |
| **Скорость (без OCR)** | ~0.5-1s | ~0.5-2s |
| **Скорость (с OCR)** | ~2-5s/изображение | ~3-10s/страница |
| **Временные файлы** | Изображения + .docx | ❌ Нет |
| **Очистка** | ✅ Обязательная | ❌ Не требуется |

## 🎓 Рекомендации по использованию

### Когда использовать OCR
- ✅ Документы с отсканированными подписями/печатями
- ✅ Вложенные изображения с текстом (диаграммы, схемы)
- ✅ Гибридные документы (текст + сканы)
- ❌ Чисто текстовые документы (overhead)

### Оптимизация производительности
1. **Отключить OCR** для текстовых документов: `enable_ocr=False`
2. **Использовать `strategy='fast'`** для массовой обработки
3. **Предконвертировать .doc в .docx** заранее (если возможно)
4. **Увеличить worker'ы Celery** для параллельной обработки

### Обработка legacy формата .doc
- **Рекомендация**: Конвертировать в .docx перед загрузкой в систему
- **Если невозможно**: Парсер автоматически конвертирует (с overhead)
- **Ограничение**: Метаданные могут быть неполными
