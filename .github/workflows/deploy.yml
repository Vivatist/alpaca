# CI/CD Pipeline для ALPACA
# 
# Триггер: push в main
# Действия:
#   1. Git pull на сервере
#   2. Пересборка и перезапуск всех контейнеров
#
# Требования:
#   1. Self-hosted runner на сервере
#   2. .env файл в ~/alpaca/services с секретами

name: Deploy to Server

on:
  push:
    branches: [main]
  
  # Ручной запуск
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: Pull latest code
        run: |
          cd ~/alpaca
          git fetch origin main
          git reset --hard origin/main
          echo "✅ Code updated to $(git rev-parse --short HEAD)"
      
      - name: Check Supabase and fix DATABASE_URL
        run: |
          cd ~/alpaca/services
          
          # Определяем порт Supabase
          SUPABASE_PORT=$(docker port supabase-db 5432 2>/dev/null | grep -oE '[0-9]+$' || echo "")
          
          if [ -z "$SUPABASE_PORT" ]; then
            echo "⚠️ supabase-db container not found, checking for postgresql..."
            SUPABASE_PORT=$(docker ps --format '{{.Ports}}' | grep -oE '0.0.0.0:[0-9]+->5432' | grep -oE ':[0-9]+' | tr -d ':' | head -1)
          fi
          
          echo "📊 Detected Supabase PostgreSQL port: ${SUPABASE_PORT:-NOT FOUND}"
          
          # Исправляем DATABASE_URL
          if [ -n "$SUPABASE_PORT" ] && [ -f .env ]; then
            # Заменяем supabase-db:5432 на 172.17.0.1:PORT
            if grep -q "supabase-db" .env; then
              sed -i "s|supabase-db:5432|172.17.0.1:${SUPABASE_PORT}|g" .env
              echo "✅ Fixed: supabase-db:5432 → 172.17.0.1:${SUPABASE_PORT}"
            fi
            # Исправляем неправильный порт 54322 если нужно
            if grep -q ":54322" .env && [ "$SUPABASE_PORT" != "54322" ]; then
              sed -i "s|:54322|:${SUPABASE_PORT}|g" .env
              echo "✅ Fixed port: 54322 → ${SUPABASE_PORT}"
            fi
          fi
          
          echo "📋 Current .env configuration:"
          grep -E "^(DATABASE_URL|OLLAMA_BASE_URL)=" .env | sed 's/:[^:@]*@/:***@/' || echo "No .env found"
      
      - name: Rebuild and restart services
        run: |
          cd ~/alpaca/services
          echo "🔄 Rebuilding containers..."
          docker compose down
          docker compose build --no-cache
          docker compose up -d
          echo "✅ Services restarted"
      
      - name: Show container status
        run: |
          echo "📊 Container status:"
          docker ps --format "table {{.Names}}\t{{.Status}}" | grep -E "alpaca|supabase" || true
      
      - name: Cleanup old images
        run: docker image prune -f
      
      - name: Health check
        run: |
          echo "⏳ Waiting for services to start..."
          sleep 15
          
          FAILED=0
          
          echo "🔍 Checking admin-backend..."
          if curl -sf http://localhost:8080/health > /tmp/health_admin.json 2>&1; then
            echo " ✅ OK"
          else
            echo " ❌ FAILED"
            docker logs alpaca-admin-backend-1 --tail 20 2>&1 || true
            FAILED=1
          fi
          
          echo "🔍 Checking chat-backend..."
          if curl -sf http://localhost:8082/health > /tmp/health_chat.json 2>&1; then
            echo " ✅ OK"
          else
            echo " ❌ FAILED"
            docker logs alpaca-chat-backend-1 --tail 20 2>&1 || true
            FAILED=1
          fi
          
          echo "🔍 Checking filewatcher..."
          if curl -sf http://localhost:8081/health > /tmp/health_fw.json 2>&1; then
            echo " ✅ OK"
          else
            echo " ❌ FAILED"
            docker logs alpaca-filewatcher-1 --tail 20 2>&1 || true
            FAILED=1
          fi
          
          echo "🔍 Checking mcp-server..."
          if curl -sf http://localhost:8083/docs > /dev/null 2>&1; then
            echo " ✅ OK"
          else
            echo " ❌ FAILED"
            docker logs alpaca-mcp-server-1 --tail 20 2>&1 || true
            FAILED=1
          fi
          
          echo ""
          echo "📊 Container status:"
          docker ps --format "table {{.Names}}\t{{.Status}}" | grep alpaca || true
          
          if [ $FAILED -eq 1 ]; then
            echo "⚠️ Some services failed health check"
            exit 1
          fi
          
          echo "✅ All services healthy!"
