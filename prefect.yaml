# Prefect deployment configuration
name: alpaca-rag
description: RAG система для обработки документов

# Pull step - где брать код
pull:
  - prefect.deployments.steps.set_working_directory:
      directory: /home/alpaca/alpaca

# Build step - подготовка окружения (опционально)
build: null

# Push step - где хранить артефакты (опционально)
push: null

# Deployments
deployments:
  - name: document-processing
    version: 2.0.0
    tags:
      - rag
      - documents
    description: Основной flow для обработки документов
    schedule: null
    work_pool:
      name: default-agent-pool
